namespace Spd.Aplicacion;

/// <summary>Qué timestamp `impreso_*_en` actualiza `RegistrarImpresion` (FR-680/681, research.md
/// Decisión 8 de Spec 006 — sin generar ningún fichero todavía).</summary>
public enum TipoDocumentoSpd
{
    Ficha,
    Etiquetas,
    Instrucciones
}
